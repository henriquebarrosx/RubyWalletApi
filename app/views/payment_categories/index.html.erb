<section class="grid grid-cols-2 gap-4 p-20 h-fit">
  <div class="flex flex-col">
    <div class="flex flex-col gap-6">
      <%= turbo_frame_tag "payment_category_form_frame", src: new_payment_category_url %>
    </div>
  </div>

  <%= render partial: "categories", locals: { payment_categories: @payment_categories } %>
</section>